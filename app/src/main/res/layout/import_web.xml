<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent"
              android:orientation="vertical">
    <LinearLayout
        android:id="@+id/contentBlock"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:nextFocusRight="@+id/import_web_cancel"
        android:nextFocusDown="@+id/import_web_cancel"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/bar_color"
        android:focusable="true"
        android:clickable="true"
        android:orientation="horizontal" >
        <Button
            android:id="@+id/import_web_cancel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_margin="3dp"
            android:paddingRight="10dp"
            android:paddingLeft="10dp"
            android:background="@drawable/button_nav"
            android:textColor="@android:color/white"
            android:drawableLeft="@drawable/ic_menu_back"
            android:drawableStart="@drawable/ic_menu_back"
            android:focusable="true"
            android:clickable="true"
            android:nextFocusLeft="@+id/webView"
            android:nextFocusUp="@+id/webView"
            android:nextFocusRight="@+id/import_web_import"
            android:nextFocusDown="@+id/import_web_import"
            android:text="@string/view_note_button_back" />
        <Button
            android:id="@+id/import_web_import"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_margin="3dp"
            android:paddingRight="10dp"
            android:paddingLeft="10dp"
            android:background="@drawable/button_nav"
            android:textColor="@android:color/white"
            android:drawableLeft="@android:drawable/ic_menu_save"
            android:drawableStart="@android:drawable/ic_menu_save"
            android:focusable="true"
            android:clickable="true"
            android:nextFocusLeft="@+id/import_web_cancel"
            android:nextFocusUp="@+id/import_web_cancel"
            android:nextFocusRight="@+id/webView"
            android:nextFocusDown="@+id/webView"
            android:text="@string/config_import" />
    </LinearLayout>
    </LinearLayout>

    <!--progress bar clock-->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/background_dark"
        android:gravity="center">
        <ProgressBar
            android:id="@+id/import_progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />
    </LinearLayout>

</LinearLayout>
